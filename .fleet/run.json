{
    "configurations": [
        {
            "name": "fast (server)",
            "type": "fastapi",
            "workingDir": "$PROJECT_DIR$",
            "environment": {
                "PYTHONPATH": "$PROJECT_DIR$"
            },
            "module": "server.main",
            "application": "app",
            "arguments": ["--reload", "--port=8000"]
        }
    ]
}